# handoff 2026-09-02 — Hermes実況の実機不合格

## HUMAN判断待ち

1. PR #464 は Draft のまま維持し、次回は同じPRで抽出処理を修正する。現時点では Ready化・mergeしない。
2. PR #464の再実機確認が合格した後に、PR #459・#463・#464のReady化とmerge順を判断する。
3. Release、タグ、DMGは引き続き変更しない。

## 現在地

- latest main: `e7a68a90e7cb85c93e48dff46cb16995e3c75b43`
- PR #459 `fix/macos-ime-input`: head `51f690ae75876f7753931639f8b5fa74c8741edb`。latest main取り込み済み、CI/CodeQL成功。旧headでのHUMAN入力Smokeは合格。
- PR #463 `fix/461-hermes-session-lifecycle`: head `b9fba773d7efa63404cd9134389ce5be950556ca`。latest main取り込み済み、CI/CodeQL成功。HUMAN確認では通常の中断、終了済み表示、Desktop Server継続、再Start、bash exit 0が合格。強制終了の `signal=N` 表示は実機未観測で、自動テストのみ確認済み。
- PR #464 `fix/460-hermes-commentary-observation`: head `e522996dc7662213fcde543d8b864230de9afdfe`。latest main取り込み済み、CI/CodeQL成功。ただしHermes v0.20の実機確認は不合格。
- PR #465: PR #464に積み重なるため未接触。#464より先にmergeしない。
- Issue #449: 未着手。PR #463と同じServer領域に触れるため、引き続き後回し。

## 2026-09-02の実機結果

安全な読み取り専用タスクで、Hermes左ターミナルには `read_file` 5件と `terminal` 1件が表示された。一方、作業実況は0件だった。

- PTY出力はServerとWebSocketへ到達していた。
- start、応答ストリーム、長時間無出力のイベントは生成された。
- tool activityイベントだけが0件だった。
- commentary生成、WebSocket送信、Web UI、TTSは受信済みイベントに対して正常だった。
- ファイル名、行番号、引数、題目、秘密情報の漏えいはなかった。
- 障害箇所は `apps/server/src/rulesets/hermes.ts` のruleset抽出段と判断する。

原因候補は未確認。実際のHermes v0.20出力とfixtureの形式差、または `\r`・カーソル移動・frame分割を伴う再描画を `normalizeStream` が行へ再構築できていない可能性が高い。

## 次回のOpenCode実装指示

1. Git、GitHub、Todoistを読み取り、latest main、PR #464 head、Draft状態、作業ツリーのcleanを確認する。
2. 新しいPRを作らず、`fix/460-hermes-commentary-observation` / PR #464で続行する。latest mainが更新されていれば先に取り込む。
3. 実機出力の本文・コマンド・引数・パス・環境変数・認証情報を保存しない。必要最小限の匿名化したcode point、frame境界、行再構築の形だけを診断証拠として取得する。
4. 実出力のframe分割、`\r`再描画、カーソル移動を再現する匿名fixtureを追加し、現行実装で失敗するテストを先に作る。
5. `apps/server/src/rulesets/hermes.ts` の正規化・行再構築・tool row検出を、得られた証拠に限定して修正する。
6. 少なくとも search/read/terminal/generic、分割frame、同一カテゴリ抑制、題目変化後の再発火、kaomoji思考、禁止情報非転送をテストする。
7. 対象テスト、Server/Web全体テスト、typecheck、lint/build、failure regression、desktop/Tauri、internal-release検証を実行する。
8. 同じPRへcommit・pushし、local head、remote head、GitHub PR headの一致とCI/CodeQLを確認する。Draftのまま停止する。
9. HUMANが短い実機Smokeを再実行し、tool activity実況が出ること、過剰連続実況がないこと、禁止情報が出ないことを確認する。

## 禁止事項

- mainへの直接commit/push
- PRのReady化・merge
- Release、タグ、DMG操作
- PR #447、#459、#463、#465、Issue #449へのコード変更
- raw PTY本文や秘密情報をGitHub、Todoist、fixtureへ保存すること
