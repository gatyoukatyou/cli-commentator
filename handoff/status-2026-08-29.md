# handoff / status 2026-08-29

- 実装担当: OpenCode
- Ref: PR #459, Issue #460 / #461 / #462
- 基準main: `50b72bec7d9ba810a6bb0e3d4785172cedc0a775`

## HUMAN判断待ち

1. PR #463（#461対応）とPR #464（#460対応）はDraftのまま。Ready化・mergeはHUMAN判断。
2. #463の実機確認: Hermes強制終了（中断ボタン2段階目）→ `終了 signal=` 実況 → 終了済み表示 → 再Start。
3. #464の実機確認: Hermes 20〜30分実況の密度が実用範囲か（過密なら調整する）。
4. PR #459: IME項目は実機PASS。#460/#461は回帰ではないため、Ready化の判断はHUMANへ委ねる。

## Gate 0 — 回帰判定

- #461 / #460 とも **main既存の独立問題（A）**。
- 根拠: #459のdiffはweb入力経路のみでServer側に到達しない。加えてローカル実証:
  - node-pty 1.1.0は直接の子終了を確実に検知（孫プロセスがTTYを掴んでいても発火）
  - Hermes v0.20.5は完了画面表示後もプロセスが生き続ける（TTS評価ログに終了イベント無し=onExit未発火）
  - Hermes v0.20.5の生出力42.7KBをmainの抽出パイプラインに通すと2イベントのみ（旧表示形式前提のrulesetが全廃棄）

## Issue #461 → Draft PR #463

- branch: `fix/461-hermes-session-lifecycle` / commit `5def395`
- 原因: (1) `PTYManager.kill()`がcurrentを先にnull化し、onExitのcurrent判定でdone通知・状態遷移が欠落する経路 (2) 終了済み表示がWebに無い (3) 中断＝単発Ctrl+CではHermesを停止できない
- 修正: 新WSメッセージ`stopSession`（子PIDのみkill、onExit経路で状態遷移）、signal終了の`終了 signal=N`区別、Web終了済み表示、中断ボタン2段階エスカレーション（10秒以内の2回目で強制終了）
- signal構造は新設せず既存kill経路を再利用。案B（常に\x03×2）と案C（process group）は不採用、PR本文に比較を記録
- tests: session-stop.integration.test.ts（SIGINT無視CLIで強制停止/Server継続/二重stop/再Start）、web 9件
- CI: Green（ci + CodeQL）

## Issue #460 → Draft PR #464

- branch: `fix/460-hermes-commentary-observation` / commit `5777f5b`
- raw evidence: 実Hermes v0.20.5のPTY生出力を匿名化し `apps/server/test/fixtures/hermes/tui-tool-activity.log` へ
- 原因: rulesetが旧表示形式前提。v0.20の絵文字ツール行（`┊ 🔎`等、区切り線連結）・単一`─`の作業題目・kaomoji思考インジケータがすべて一致せず廃棄されていた
- 修正: ツール行カテゴリルール（同カテゴリ連続は1回、題目/プロンプト変化で再武装）、題目変化ルール（テキストは転送しない）、kaomoji思考をresponse-stream fingerprintへ統合
- fixture再生: 42.7KB/90秒 → 9イベント。プライバシー（args・原文・題目・ステータスバー内容非転送）をテストで固定
- CI: Green（ci + CodeQL）

## PR #459

- 変更したか: no（read-onlyコメントのみ [#issuecomment-5462563484](https://github.com/gatyoukatyou/cli-commentator/pull/459#issuecomment-5462563484)）
- merge可否に影響する新たなblocking evidence: なし

## 未着手 / やらないこと

- #462（UX backlog）: 実装せず。重複事実も確認されず
- `pnpm verify:internal-release`、Release、Apple署名、#449/#450/#448、#406: 本日は未対応

## 次回の作業再開地点（2026-08-29 22:30時点の状態で停止）

- 備考: Gino（ChatGPT）が5時間利用制限中。ただし merge / Ready化 / Release は運用ルール上HUMANのみの判断のため、進行への影響なし。

### 状態（全て検証済み・停止点）

| 対象 | 状態 |
|---|---|
| PR #463（#461対応） | OPEN / Draft / checks 9/9 SUCCESS。commit `5def395`、branch `fix/461-hermes-session-lifecycle` |
| PR #464（#460対応） | OPEN / Draft / checks 9/9 SUCCESS。commit `5777f5b`、branch `fix/460-hermes-commentary-observation` |
| PR #459 | OPEN / Draft / 未変更。因果切り分けコメント済み（issuecomment-5462563484） |
| main | `50b72be` で作業ブランチに取り込み済み。ローカルmainも同期済み |

### 再開時の順路

1. HUMAN: PR #463 実機確認（Hermes強制終了→`終了 signal=`実況→終了済み表示→Desktop Server継続→再Start→bash `exit 0`回帰、の6項目）
2. HUMAN: PR #464 実機確認（Hermes 20〜30分利用で実況密度が実用範囲か。過密ならCodexへ連絡し調整）
3. HUMAN: PR #459 をReady化するか判断（blocking evidenceなし）
4. 確認結果をPRコメントへ → Codexが検証・追加修正・再CIに対応
5. merge後: `git checkout main && git pull --ff-only && git fetch --prune` でローカル同期
6. merge後に残る作業: `pnpm verify:internal-release`（#463 branch上）、Todoist親カードへの要約投稿、v0.2.2 Release判断

### Todoist

- 本handoffの要約をTodoist親カードのコメントへ投稿する必要あり。**未投稿**（Codex環境にTodoist CLI/MCPが無く、投稿はClaude Code（KURO）またはHUMANのレーン）。投稿文は本handoffの「HUMAN判断待ち」「次回の作業再開地点」をそのまま使える。
